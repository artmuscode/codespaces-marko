{
	"name": "codespaces-blank",
	"image": "mcr.microsoft.com/devcontainers/base:debian-12",
	"remoteEnv": {
		"PATH": "${containerWorkspaceFolder}/${containerEnv:MARKO_PROJECT_NAME}/vendor/bin:${containerEnv:PATH}"
	},
	"features": {
		"ghcr.io/devcontainers/features/node:2.0.0": {
			"version": "lts"
		},
		"ghcr.io/devcontainers/features/php:1": {
			"version": "latest"
		},
		"ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
	},
	"privileged": false,
	"containerEnv": {
    		"MARKO_PROJECT_NAME": "project-name",
    		"MARKO_INSTALL_MODE": "skeleton",
    		"MARKO_PACKAGES": "marko/database marko/cache-redis marko/session marko/session-file marko/view marko/view-latte marko/security marko/testing marko/database-mysql"
  	},
	"postCreateCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends default-mysql-client postgresql-client && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/* && bash .devcontainer/init-marko.sh",
	"postStartCommand": "bash .devcontainer/init-marko.sh",
	"forwardPorts": [
		8000
	],
	"portsAttributes": {
		"8000": {
			"label": "App Server"
		}
	},
	"remoteUser": "vscode"
}
